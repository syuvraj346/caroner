# Shared Config

This package is reserved for reusable project configuration.

Expected future contents:
- ESLint shared config
- TypeScript base config
- prettier config
- environment schema validation
- shared constants
