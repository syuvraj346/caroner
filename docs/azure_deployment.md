# Azure Deployment

See root-level `azure_deployment.md` for the current deployment planning document.

This file can later become the canonical copy once documentation is consolidated under `docs/`.
